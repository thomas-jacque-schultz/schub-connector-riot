package schultz.thomas.schub.connector.riot.data.model.riot;

/** Forme brute de {@code account-v1} — route <strong>régionale</strong>. */
public record RiotAccountResponse(String puuid, String gameName, String tagLine) {
}
