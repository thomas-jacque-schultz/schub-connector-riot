package schultz.thomas.schub.connector.riot.api.dto;

/** L'axe de regroupement. Le connecteur compte ; ce que le regroupement veut dire est du cœur. */
public enum StatsGrouping {

    OVERALL,
    CHAMPION,
    POSITION,
    QUEUE,
    PATCH,
    MONTH,
    SIDE
}
